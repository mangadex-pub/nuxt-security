{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "3.8.0",
    "vite": "^4.0.0"
  },
  "dependencies": {
    "nuxt-security": "^1.0.0-rc.2"
  }
}
